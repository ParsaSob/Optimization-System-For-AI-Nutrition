# Railway Environment Variables
# Copy this to .env file or set in Railway dashboard

PORT=8000
HOST=0.0.0.0
PYTHON_VERSION=3.11.9
NODE_ENV=production

# API Configuration
API_HOST=0.0.0.0
API_PORT=8000
API_RELOAD=false

# Logging
LOG_LEVEL=info
LOG_FORMAT=%(asctime)s - %(name)s - %(levelname)s - %(message)s

# Database
DATABASE_URL=sqlite:///./meal_optimization.db

# CORS
ALLOWED_ORIGINS=["*"]
ALLOWED_METHODS=["*"]
ALLOWED_HEADERS=["*"]

